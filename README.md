# Prestashop Inventory Builds

This repository contains only public distribution artifacts for the `prestashopinventory` module.

Files:
- `latest.json` - signed update manifest used by the module updater
- `prestashopinventory-v*.zip` - install/update packages
